<section id="splash" class="panel">
<img id="splashImg" src="/images/bckgrnd.png" alt="">
</section>


    <div class="team">
        
        {{#each people}}
        <div class="user-panel">
            <img class="icon" src="images/{{ Image }}" alt="icon">

        

            <h2>{{ Title }}</h2>
            <h4>{{ Description }}</h4>

    <div class="dropdown">
        <button>Did You Know?</button>
     <div class="dropdown-content">
        <h4>{{ Fact }}</h4>
  </div>
</div>
            
            <h5> <a href = "{{ Link }}" target = "blank">Link</a></h5>
        </div>
        {{/each}}
        
    </div>
    </div>

    <!-- this will be filled in on the client side when we render our AJAX data -->
    <section class="lightbox">
        <span class="close">x</span>
        <div class="sidebar">
            <img src="" alt="larger bio image">
        </div>

        <div class="lb-content">

        </div>
    </section>